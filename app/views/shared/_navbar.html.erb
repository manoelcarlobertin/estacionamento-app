<nav class="navbar fixed-top navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">
      <i class="bi bi-wallet2"></i> Rails Estacionamento App
    </a>
    <button class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#navMenu">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navMenu">
      <ul class="navbar-nav ms-auto">
        <% unless user_signed_in? %>
          <li class="nav-item me-2">
            <%= link_to "Entrar" , new_user_session_path,
              class: "btn btn-outline-light nav-btn" %>
          </li>
        <% else %>
          <li class="nav-item me-2">
            <%= button_to "Sair" , destroy_user_session_path,
              method: :delete, class: "btn btn-outline-light nav-btn" %>
          </li>
        <% end %>
        <li class="nav-item me-2">
          <%= link_to "Dashboard" , root_path, class: "btn btn-outline-light nav-btn" %>
        </li>
        <li class="nav-item me-2">
          <%= link_to "Adicionar Vaga" , new_vaga_path,
            class: "btn btn-outline-light nav-btn" %>
        </li>
        <li class="nav-item me-2">
          <%= link_to "Adicionar Veículo" , new_veiculo_path,
            class: "btn btn-outline-light nav-btn" %>
        </li>

      </ul>
    </div>
  </div>
</nav>